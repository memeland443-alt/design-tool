# Инструкция по запуску проекта

## ✅ Что уже сделано

Проект полностью настроен и готов к работе:

- ✅ Установлены все зависимости
- ✅ Настроен Replicate API
- ✅ Сервер запущен на http://localhost:3000
- ✅ API токен добавлен в .env.local

## 🎨 Что можно делать сейчас

1. Откройте браузер и перейдите на http://localhost:3000
2. Выберите таб "Remove BG"
3. Перетащите изображения в область загрузки или нажмите для выбора файлов
4. Нажмите "Remove Background"
5. Скачайте обработанные изображения

## 📂 Структура проекта

```
design.tool/
├── app/
│   ├── api/remove-background/    # API endpoint для удаления фона
│   ├── page.tsx                  # Главная страница с табами
│   ├── layout.tsx                # Layout приложения
│   └── globals.css               # Глобальные стили
├── components/
│   ├── background-remover.tsx    # Компонент загрузки и обработки
│   └── ui/                       # shadcn/ui компоненты
├── lib/
│   └── utils.ts                  # Утилиты
└── .env.local                    # Переменные окружения (ваш API токен)
```

## 🚀 Команды для работы

```bash
# Запустить dev сервер
npm run dev

# Собрать для production
npm run build

# Запустить production сервер
npm start

# Проверить код
npm run lint
```

## 🔮 Следующие шаги для расширения

### Добавить новую функцию (например, Upscale)

1. Создайте API роут: `app/api/upscale/route.ts`
2. Добавьте компонент: `components/upscale.tsx`
3. Обновите страницу: `app/page.tsx` - уберите `disabled` с таба "Upscale"

### Найти другие модели на Replicate

Посетите https://replicate.com/explore и найдите нужные модели:
- Image upscaling
- Image enhancement
- Style transfer
- Image generation
- И многое другое

## 📦 Деплой на Vercel

1. Создайте репозиторий на GitHub
2. Загрузите проект: `git init && git add . && git commit -m "Initial commit"`
3. Перейдите на https://vercel.com
4. Импортируйте репозиторий
5. Добавьте переменную окружения: `REPLICATE_API_TOKEN`
6. Нажмите Deploy

## 🐛 Возможные проблемы

### Ошибка "Invalid version or not permitted"
- Проверьте, что API токен правильно указан в `.env.local`
- Убедитесь, что модель существует на Replicate

### Порт 3000 занят
- Остановите другие процессы: `taskkill //F //PID <PID>`
- Или используйте другой порт: `PORT=3001 npm run dev`

### Rate limit exceeded
- Replicate имеет лимиты на бесплатном плане
- Подождите несколько минут или обновите план

## 📝 Лицензия

MIT
