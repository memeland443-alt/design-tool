# 🔧 Решение проблем

## Проблема: "Waiting..." не меняется на результат

### Проверьте правильный порт
1. В консоли сервера найдите строку: `Local: http://localhost:XXXX`
2. Убедитесь, что браузер открыт на том же порту
3. Если порты не совпадают - перезагрузите страницу на правильном порту

### Проверьте логи сервера
После нажатия "Remove Background" в консоли сервера должны появиться:
```
📥 Received background removal request
📸 Processing image: filename.jpg (12345 bytes, image/jpeg)
✅ Image converted to base64 (67890 chars)
🚀 Starting Replicate prediction...
⏳ Prediction created: p-xxxxx, status: starting
⏳ Still processing... (5s) status: processing
✅ Prediction succeeded!
```

Если логов нет:
- ❌ Запрос не доходит до API
- ✅ Проверьте Network tab в DevTools
- ✅ Убедитесь, что порт правильный

### Проверьте API токен
```bash
# Откройте .env.local
# Должна быть строка:
REPLICATE_API_TOKEN=r8_xxxxxxxxxxxxxxxxxxxxx

# Токен должен начинаться с "r8_"
```

### Проверьте размер изображения
Слишком большие изображения могут вызвать проблемы:
- ✅ Рекомендуется: < 5MB
- ⚠️ Может быть медленно: 5-10MB
- ❌ Может не работать: > 10MB

## Проблема: Ошибка "Invalid version or not permitted"

### Причины:
1. **Неверный API токен**
   ```bash
   # Получите новый токен:
   # https://replicate.com/account/api-tokens
   ```

2. **Модель изменила версию**
   ```bash
   # Проверьте актуальную версию:
   # https://replicate.com/bria/remove-background
   ```

3. **Rate limit**
   ```
   Подождите несколько минут
   ```

## Проблема: "Processing timed out"

### Решения:
1. **Уменьшите размер изображения**
   - Сжмите перед загрузкой
   - Рекомендуется: max 2048x2048px

2. **Увеличьте timeout**
   ```typescript
   // В route.ts измените:
   const maxAttempts = 60  // на 120 для 2 минут
   ```

3. **Проверьте подключение к интернету**

## Проблема: Порт 3000 уже занят

```bash
# Windows:
netstat -ano | findstr :3000
taskkill //F //PID <PID>

# Или запустите на другом порту:
# package.json -> scripts -> dev:
"dev": "next dev -p 3001"
```

## Проблема: "Module not found"

```bash
# Переустановите зависимости:
rm -rf node_modules
rm package-lock.json
npm install
```

## Проблема: TypeScript ошибки

```bash
# Очистите кеш Next.js:
rm -rf .next
npm run dev
```

## Проверка работы API вручную

### 1. Проверка здоровья сервера
Откройте в браузере: `http://localhost:3000`

Должна открыться страница с табами.

### 2. Проверка API endpoint
```bash
# Используйте curl или Postman:
curl -X POST http://localhost:3000/api/remove-background \
  -F "image=@path/to/your/image.jpg"
```

Должен вернуться JSON с полем `output`.

### 3. Проверка Replicate API напрямую
```bash
curl -X POST "https://api.replicate.com/v1/predictions" \
  -H "Authorization: Bearer $REPLICATE_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "version": "1a075954106b608c3671c2583e10526216f700d846b127fcf01461e8f642fb48",
    "input": {
      "image": "https://example.com/image.jpg"
    }
  }'
```

## Получение подробных логов

### В API route добавьте больше логов:
```typescript
console.log('Full prediction:', JSON.stringify(prediction, null, 2))
console.log('Error details:', error)
```

### Включите verbose логи Next.js:
```bash
DEBUG=* npm run dev
```

## Часто задаваемые вопросы

**Q: Почему обработка занимает так долго?**
A: Replicate обрабатывает изображения на удаленных серверах. Обычно это занимает 5-15 секунд.

**Q: Можно ли обрабатывать несколько изображений одновременно?**
A: Да, но каждое будет обрабатываться последовательно из-за архитектуры API route.

**Q: Сколько стоит обработка?**
A: Зависит от вашего плана на Replicate. Проверьте: https://replicate.com/pricing

**Q: Можно ли использовать другие модели?**
A: Да! См. [REPLICATE_GUIDE.md](./REPLICATE_GUIDE.md) для примеров.

## Контакты поддержки

- [Replicate Support](https://replicate.com/docs)
- [Next.js Discord](https://nextjs.org/discord)
- [GitHub Issues](https://github.com/anthropics/claude-code/issues)
