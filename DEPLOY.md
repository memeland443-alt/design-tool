# 🚀 Инструкция по деплою на Vercel

## Шаг 1: Создайте репозиторий на GitHub

### Вариант A: Через веб-интерфейс GitHub
1. Откройте https://github.com/new
2. Назовите репозиторий (например: `design-tool`)
3. Оставьте репозиторий **Public** или **Private** (на ваш выбор)
4. **НЕ** добавляйте README, .gitignore или license (у нас они уже есть)
5. Нажмите "Create repository"

### Вариант B: Через GitHub CLI (если установлен)
```bash
gh repo create design-tool --public --source=. --remote=origin
```

## Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория на GitHub, выполните команды:

```bash
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/design-tool.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/design-tool.git

# Запушьте код
git branch -M main
git push -u origin main
```

## Шаг 3: Деплой на Vercel

### Способ 1: Через веб-интерфейс Vercel (рекомендуется)

1. **Откройте Vercel**
   - Перейдите на https://vercel.com
   - Войдите или зарегистрируйтесь (можно через GitHub)

2. **Импортируйте проект**
   - Нажмите "Add New..." → "Project"
   - Выберите ваш GitHub репозиторий `design-tool`
   - Нажмите "Import"

3. **Настройте проект**
   - **Project Name**: `design-tool` (или любое имя)
   - **Framework Preset**: Next.js (определится автоматически)
   - **Root Directory**: `./` (оставьте как есть)
   - **Build Command**: `npm run build` (оставьте по умолчанию)
   - **Output Directory**: `.next` (оставьте по умолчанию)

4. **Добавьте Environment Variables**

   В разделе "Environment Variables" добавьте:

   | Name | Value |
   |------|-------|
   | `REPLICATE_API_TOKEN` | `your_replicate_api_token_here` |

   ⚠️ **ВАЖНО**: Замените на ваш актуальный Replicate API токен из https://replicate.com/account/api-tokens

5. **Деплой**
   - Нажмите "Deploy"
   - Дождитесь завершения (обычно 1-2 минуты)
   - Получите URL вида: `https://design-tool-xxx.vercel.app`

### Способ 2: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Деплой
vercel

# Следуйте инструкциям:
# - Set up and deploy? Yes
# - Which scope? [Ваш аккаунт]
# - Link to existing project? No
# - Project name? design-tool
# - Directory? ./
# - Override settings? No

# Добавьте environment variable
vercel env add REPLICATE_API_TOKEN

# Продакшн деплой
vercel --prod
```

## Шаг 4: Проверка деплоя

После успешного деплоя:

1. **Откройте ваш сайт**
   - Vercel покажет URL: `https://your-project.vercel.app`
   - Откройте его в браузере

2. **Проверьте функционал**
   - ✅ Страница загружается
   - ✅ Табы работают
   - ✅ Можно загружать изображения
   - ✅ Удаление фона работает

3. **Проверьте логи**
   - Откройте Vercel Dashboard
   - Перейдите в проект → Runtime Logs
   - Попробуйте обработать изображение
   - Проверьте, что появляются логи с эмодзи

## 🔄 Автоматические деплои

После настройки, каждый push в GitHub будет автоматически деплоиться:

```bash
# Внесите изменения
git add .
git commit -m "Add new feature"
git push

# Vercel автоматически создаст новый деплой!
```

## 🌐 Настройка кастомного домена (опционально)

1. В Vercel Dashboard откройте проект
2. Перейдите в "Settings" → "Domains"
3. Добавьте ваш домен
4. Настройте DNS записи (Vercel покажет инструкции)

## 👥 Приглашение команды

### В Vercel:
1. Settings → Team
2. "Invite Member"
3. Укажите email коллег

### В GitHub:
1. Settings → Collaborators
2. "Add people"
3. Укажите username коллег

## 📊 Мониторинг и аналитика

Vercel предоставляет:
- **Analytics**: Количество запросов, производительность
- **Speed Insights**: Метрики загрузки страниц
- **Runtime Logs**: Логи выполнения функций
- **Deployments**: История всех деплоев

Доступ: https://vercel.com/dashboard

## 🔧 Управление переменными окружения

### Добавить новую переменную:
1. Vercel Dashboard → Project → Settings → Environment Variables
2. Add New → Укажите имя и значение
3. Выберите окружения (Production, Preview, Development)
4. Save

### Обновить существующую:
1. Найдите переменную в списке
2. Edit → Измените значение
3. Save
4. **Важно**: Нужно пересоздать деплой для применения

```bash
# Через CLI
vercel env rm REPLICATE_API_TOKEN
vercel env add REPLICATE_API_TOKEN
vercel --prod
```

## 🐛 Troubleshooting

### Ошибка при деплое
- Проверьте логи в Vercel Dashboard
- Убедитесь, что `npm run build` работает локально
- Проверьте, что все зависимости в `package.json`

### API не работает
- Проверьте Environment Variables в Vercel
- Убедитесь, что токен правильный
- Проверьте Runtime Logs

### Медленная загрузка
- Vercel автоматически использует CDN
- Картинки кешируются через Replicate CDN
- Edge Functions работают глобально

## 📞 Полезные ссылки

- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel Documentation](https://vercel.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [GitHub Repository Settings](https://github.com/YOUR_USERNAME/design-tool/settings)

## 🎉 Готово!

Теперь ваше приложение доступно онлайн по адресу:
```
https://your-project.vercel.app
```

Поделитесь ссылкой с командой и начинайте тестировать! 🚀
